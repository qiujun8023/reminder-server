get:
  tags: [ setting ]
  summary: 获取提醒详情
  security:
  - wechat: []
  parameters:
    - name: settingId
      in: query
      type: integer
      required: true
      description: 设置 Id
  responses:
    200:
      description: 设置列表
      schema:
        $ref: "#/definitions/Setting"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
