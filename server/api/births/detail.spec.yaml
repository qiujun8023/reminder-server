get:
  tags: [ birth ]
  summary: 获取生日详情
  security:
  - wechat: []
  parameters:
    - name: birthId
      in: query
      type: integer
      required: true
      description: 生日 Id
  responses:
    200:
      description: 生日列表
      schema:
        $ref: "#/definitions/Birth"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
